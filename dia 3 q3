#include <stdio.h>
#include <string.h>

int main()
{
   int i,letra;
   char p[20];

        scanf("%s",p);
        letra = strlen(p);

   for(i=0;i<letra;i++)
   {
       if(p[i]=='p')
       {
           p[i]= '\0';
       }
   }
        printf("%s\n",p);



return 0;
}
