#include <stdio.h>


int main()
{
    int v[12];
    int i,cont=0,num;

    for(i=0;i<12;i++)
    {
      scanf("%i", &v[i]);
    }

        for(i=0;i<12;i++)
        {
           if(v[i]%2==0)
        {
            cont==cont++;
        }

        }
            printf("%i  valores pares\n",cont );

                for(i=0;i<12;i++)
                {
                   if(v[i]%2==0)
                {
                     printf("%i",v[i]);
                }
                }



return 0;
}
