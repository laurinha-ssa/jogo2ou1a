#include <stdio.h>
#include <string.h>


int main()
{
   int i,letra;
   char v[20];

        scanf("%s",v);
        letra = strlen(v);

   for(i=0;i<letra;i++)
   {
       if(v[i]=='p')
       {
           v[i]= '\0';
       }
   }
        printf("%s\n",v);



return 0;
}
